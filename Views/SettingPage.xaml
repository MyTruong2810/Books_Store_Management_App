<Page
    x:Class="Books_Store_Management_App.Views.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Books_Store_Management_App.Views"
    xmlns:Converters="using:Books_Store_Management_App.Helpers" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    Background="{StaticResource BackgroundColor}">

    <Page.Resources>
        <!-- Đảm bảo rằng ThemeToBoolConverter được đăng ký trong Resources -->
        <Converters:ThemeToBoolConverter x:Key="ThemeToBoolConverter"/>
    </Page.Resources>

    <Grid Padding="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="Dark Mode" FontSize="16" />
            <ToggleSwitch
                x:Name="ToggleSwitch"
                IsOn="{Binding IsDarkModeEnabled, Mode=OneWay}" 
                Toggled="ToggleSwitch_Toggled" />
        </StackPanel>
    </Grid>
</Page>
